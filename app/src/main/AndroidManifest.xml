<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.project.systemcontrol_smartlight">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_scroll"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_scroll"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpandableListview"
        tools:replace="android:icon"> <!-- Added tools:replace attribute here -->
        <activity android:name="com.project.SLCAM.TrafficLightDetailActivity"></activity>
        <activity android:name="com.project.SLCAM.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
